<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="30dp"
    android:background="#F5F7F4">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pharmacist Login"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#2F855A"
        android:layout_marginBottom="40dp"/>

    <EditText
        android:id="@+id/pharmacistEmail"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:hint="Email"/>

    <EditText
        android:id="@+id/pharmacistPassword"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:hint="Password"
        android:inputType="textPassword"
        android:layout_marginTop="20dp"/>

    <Button
        android:id="@+id/pharmacistLoginButton"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:text="Login"
        android:textAllCaps="false"
        android:backgroundTint="#2F855A"
        android:layout_marginTop="30dp"/>

</LinearLayout>